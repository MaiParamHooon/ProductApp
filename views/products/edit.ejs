<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta http-equiv="X-UA-Compatible" content="IE=edge" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title>Edit</title>
	</head>
	<body>
		<h1>Edit a product</h1>
		<form action="/products/<%= productFound._id%>?_method=PUT" method="post">
			<label for="name">Name</label>
			<input
				type="text"
				id="name"
				name="name"
				placeholder="poduct Name"
				value="<%= productFound.name %>"
			/>
			<label for="price">Price</label>
			<input
				type="text"
				id="price"
				name="price"
				placeholder="Product Price"
				value="<%= productFound.price%>"
			/>
			<label for="category">Category</label>
			<select name="category" id="category">
				<option value="fruit" <%= productFound.category === 'fruit' ? 'selected' : ' '%>>Fruit</option>
				<option value="vegetable" <%= productFound.category === 'vegetable' ? 'selected' : ' '%>>Vegetable</option>
				<option value="dairy" <%= productFound.category === 'dairy' ? 'selected' : ' '%>>Dairy</option>
			</select>
			<button type="submit">Submit</button>
		</form>
		<a href="/products">Cancel</a>
	</body>
</html>
